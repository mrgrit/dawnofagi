# TODO — 해야 할 것들

> 2026-08-02 기준. P0~P7 완료 후 남은 것.
> 결정이 필요한 **질문**은 [`QUESTIONS.md`](QUESTIONS.md), 지나온 기록은
> [`BUILD_LOG.md`](BUILD_LOG.md). 여기는 **아직 안 한 일**만 적는다.
>
> 항목마다 **누가** 할 수 있는지를 먼저 적었다. 에이전트가 못 하는 일이 섞여 있으면
> 목록이 진행되지 않는 이유를 아무도 모르게 된다.

---

## ✅ 닫힘

### ~~T1. 픽셀 오피스가 인증 없이 공개 URL 로 열려 있었다~~ (2026-08-02 닫음)

`make tunnel-down` 으로 **터널 2개를 모두 닫았다.** 외부 URL 은 이제 `530`
(터널 없음)이고 `cloudflared` 프로세스도 남지 않았다. 사내 접속은 그대로다
(`:8800` 오피스 · `:8810` 홈페이지 · `:8811` 그룹웨어).

무엇이 노출됐었는지는 남겨 둔다 — 다시 열 때 같은 판단을 해야 하기 때문이다:
인증 없이 `/api/state` 가 **2.4MB**(전 에이전트 텔레메트리 · 케이스 619건 ·
자산 이름 · 트레이스)를 그대로 줬다. `dawn-aoc serve` 배너의 "인증 없이 열려
있다" 경고를 무시한 채 인터넷에 붙어 있었다.

**다시 열려면** 앞에 Cloudflare Access 를 두거나 콘솔에 인증을 붙인 뒤에 한다(T2).
공개 홈페이지만 열려면 `make tunnel` (오피스는 `TARGET=office` 를 줘야 열린다).

### ~~T2. 관제 콘솔에 인증이 없다~~ (2026-08-02 붙임)

**그룹웨어 로그인을 그대로 인정한다.** 로그인을 새로 만들지 않았다 — 계정을 두 벌
두면 권한이 갈라지고, 갈라진 권한은 관리되지 않는다.

되는 이유는 **쿠키가 포트를 구분하지 않기 때문**이다. `:8811` 에서 로그인하면 같은
호스트의 `:8800` 에도 `dawn_portal` 쿠키가 간다. 콘솔은 그 서명을 검증하고
`aoc.view` 능력을 본다. **호스트 이름은 구분하므로** `localhost:8811` 로 로그인하고
`192.168.0.108:8800` 을 열면 쿠키가 안 간다 — 같은 이름으로 접속해야 한다.

* **노출될 때만 켜진다** — `--host 0.0.0.0` 이면 인증, loopback 이면 그대로.
  위험한 경우가 정확히 외부 바인딩이고, 개발 흐름은 막지 않는다.
* **끄려면 명시해야 한다** — `--no-auth` (배너가 경고를 띄운다)
* **fail closed** — 세션 키가 없으면 기동을 거부한다. "검증기가 없으니 통과"가 가장 나쁘다
* el34 브리지(10.20.x) 노출도 이걸로 함께 막힌다

다시 여는 절차는 `make web-bg` (세션 키 생성) → `make office-bg`.

---

## 🔴 지금

### T3. HTTPS 는 Cloudflare 가 끝단에서만 준다

**누가**: 사람 (인증서·도메인)

터널을 다시 열 때의 이야기다. 임시 도메인은 주소가 재기동마다 바뀌고, 사내
구간(192.168.0.108:8800/8810/8811)은 평문이다. 고정 도메인이 생기면 Named Tunnel
로 바꾼다 (`infra/cloudflare/README.md`).

---

## 🟡 사람만 할 수 있는 것 — 정보·권한이 필요

### T4. 원가 단가 4개 (Q9-③)

**누가**: 사람 (견적서·고지서) · **막는 것**: 정산이 항상 "하한"으로 나온다

`org/ratecard.yaml` 의 아래 값만 채우면 나머지는 계산된다
(`시간당 = 구입가/(수명×8760) + 소비전력kW × 전기요금`):

| 값 | 어디서 |
|---|---|
| `electricity_krw_kwh` | 한전 고지서 |
| `hardware.gpu-server.구입가` · `.소비전력w` | 견적서 · 전원부 라벨 |
| `hardware.container-host.구입가` · `.소비전력w` | 동상 (Intel N150 미니PC) |

**숫자를 지어내지 않았다.** 그럴듯한 값은 권위 있어 보이면서 틀리고, 그게 `0` 과
`미정` 을 나눈 이유였다. 채우기 전까지 원가는 **미정으로 표시되고 경비로 기표되지
않는다** — 반쪽 금액이 장부에 오르면 사실로 굳는다.

확인: `.venv/bin/python -m dawn_biz.cli close <id> --dry-run`

### T5. 클라우드 모델 경로가 미검증 (`ANTHROPIC_API_KEY` 없음)

**누가**: 사람 (키 발급) · **막는 것**: EG 가 Opus 를 배정한 조직의 실행

`.env` 에 키가 없다. `aoc-dev` 는 EG 가 클라우드 모델을 배정한 조직이라 키 없이는
못 돈다 — 실제로 작업 지시 #28 의 run 이 여기서 실패했고, **검수가 그걸 잡아서
다음 단계로 못 가게 막았다**(설계대로). 로컬 모델(사내 GPU) 경로는 정상이다.

### T6. GitHub PAT 재발급 (Q2)

**누가**: 사람 · **막는 것**: CI 활성화 (T7)

노출된 PAT 를 폐기하고 `workflow` 스코프를 포함해 재발급한다.

### T7. CI 가 꺼져 있다

**누가**: 에이전트 가능 · **선행**: T6

`make ci-enable` 이 `infra/ci/github-actions-ci.yml` 을 `.github/workflows/` 로
복사한다. 지금은 PAT 에 `workflow` 스코프가 없어 push 가 거부된다.
그때까지는 `make check` 를 사람이 돌린다.

### T8. 외부 장비가 풀에 하나도 없다 · 존 경유 미개통 (Q10)

**누가**: 사람 (하드웨어 · 방화벽)

`infra/pool.yaml` 의 `hosts: []`. 그래서 `vm`·`server` 등급 작업은 항상
`waiting_infra` 다 — **실패가 아니라 대기다.** 절차:

```bash
# 1) 장비를 pool.yaml 에 등록 (id·kind·zone·address·spec)
# 2) 컨테이너 존에 닿아야 하면 경로를 연다 (방화벽 변경 = 규칙 2, 사람이 한다)
bash infra/el34/route-external.sh            # 무엇을 바꿀지 보여만 준다
sudo bash infra/el34/route-external.sh --apply
# 3) pool.yaml 의 routing.enabled: true · zones: [dmz]
.venv/bin/python -m dawn_biz.cli infra --retry   # 멈춰 있던 지시가 이어서 돈다
```

### T9. 도커 접근 권한이 없다 (의도된 것)

**누가**: 사람 (판단) · **현재**: 컨테이너는 명령서 → 사람 집행

이 프로세스는 도커 소켓을 못 만진다. **그게 맞다** — 소켓을 쥐면 호스트 루트와
다름없다(최소권한). 그래서 컨테이너 등급은 `waiting` + `docker run …` 명령을 내고,
사람이 실행한 뒤 `dawn-biz infra <id> --confirm <누구>` 로 이어받는다.

바꾸려면 두 가지 길이 있는데 **둘 다 사람이 정할 문제다**: (a) 지금처럼 사람 집행을
유지, (b) 권한 있는 별도 프로세스(소켓 프록시)를 두고 허용 명령을 화이트리스트로 제한.
`ccc` 를 `docker` 그룹에 넣는 것은 **권하지 않는다** — 에이전트에게 루트를 주는 것과 같다.

---

## 🟢 다음 단계 — 에이전트가 할 수 있는 것

### T10. L2 없는 팀이 12개 (Q11 의 나머지)

**누가**: 에이전트 (사람 검토 필요) · **막는 것**: 그 팀에 편성 불가 (설계대로)

```
aoc-lab · aoc-ops · ax-marketing · ax-security · corp-finance · corp-hr
corp-marketing · corp-sales · corp-secretary · itops-bizsupport
itops-datacenter · itops-support
```

L2 는 그 팀 에이전트 전체의 행동 규칙이라 **자동 생성하지 않기로 했다.** 필요한
팀부터 하나씩 쓴다 — `ax-university` 가 그 방식의 본보기다(L2 + gate + L3 + L4).

**이제 서버에 안 들어가고 그룹웨어에서 쓸 수 있다** — `/control` → `team/<팀id>`.
없는 L2 는 "아직 없다"로 표시되고, 저장하면 새로 만들어진다.

**우선순위는 실제 업무가 정한다.** 다음 고객이 보안 AX 면 `ax-security`, 상시
시스템운영을 에이전트에게 넘길 거면 `itops-support` 가 먼저다.

### T11. 대학 AX 의 SOP 두 개가 비어 있다

**누가**: 에이전트 또는 사람 (`/control` → `work/…`)

`work/consulting/` 에 진단(`ax-diagnosis`)만 있다. 참조 색인이 제안한 3분해 중
나머지 둘 — 커리큘럼 설계 · 시뮬레이터 구축 — 은 **입력이 진단서라 순서가 있어서**
아직 안 썼다. 첫 실제 고객 건을 하면서 쓴다.

시뮬레이터를 만들려면 `ax-university` 게이트에 `dev.*`·`proj.*` 를 **의도적으로**
다시 열어야 한다 (지금은 최소권한으로 닫혀 있다).

### T15. 승인 큐 — 승인이 집행으로 이어지지 않는다 (2026-08-02 발견)

**누가**: 사람 (설계 판단) · **표기는 붙였다**

이 시스템의 HITL 은 **멈추고 기록**이지 **기다렸다 재개**가 아니다. 워커는 승인
요청을 넣고 그 자리에서 run 을 끝낸다(`return decision, None`). 큐를 지켜보는
것이 없으므로 **나중에 눌러도 그 행동은 집행되지 않는다.**

화면이 그 사실을 말하지 않아, 실제로 `aoc.kill` 2건이 승인된 채 남아 있었다
(확인 결과 아무 에이전트도 죽지 않았다). **이번엔 무해한 방향으로 틀렸지만
반대였다면 훨씬 나빴다** — 집행됐는데 안 됐다고 믿는 경우.

지금 한 것: 요청에 `run_ended` 를 남기고 화면·CLI 가 그 사실을 말한다.
훈련(`purpose`)을 큐에서 갈랐다. 리허설·테스트 잔여 **2,719건을 만료**시켰다
(승인도 거부도 아니다 — 아무도 본 적이 없으니 거부로 쓰면 거짓 판단 이력이 된다).

**남은 판단은 사람 몫이다:**

| 선택 | 뜻 |
|---|---|
| **지금 구조 유지** ← 기본 | 큐는 "차단 기록 + 다음 정책 판단". 집행하려면 작업을 다시 돌린다 |
| 재개 구현 | 승인 시 그 run 을 이어서 실행. 상태 보존·타임아웃·중복 실행 방지가 필요하다 |

재개는 간단하지 않다 — 승인이 며칠 뒤에 올 수 있고, 그때 세상은 달라져 있다.
**"오래된 승인으로 비가역 행동을 집행"** 이 새로운 위험이 된다.

### T12. 품질 판정 표본이 1건뿐

**누가**: **실제 업무가 돌아야 한다** (시간만으로는 안 쌓인다)

할루시네이션율 KPI 가 **100% (n=1)** 이다. 소표본 artifact 가 아니라 **진짜 실패**다 —
그 한 건이 "`doc.search` 결과 없음(근거 문서 부재)"이라고 써 놓고 없는 사내 가이드
내용을 단정했고 judge 가 근거 30점으로 잡았다. **품질 축이 제 일을 했다.**

**cron 만으로는 안 쌓인다.** 실측(2026-08-02): run 47건 중 `purpose` 가
`work` 인 것은 **1건뿐**이고 나머지는 `unknown` 44 · `drill` 2 다. `aoc.cycle` 은
수집·탐지를 돌릴 뿐, judge 는 **판정할 실업무 run 이 새로 생겨야** 돈다.

표본을 쌓으려면 둘 중 하나가 필요하다:

* **작업 지시를 실제로 돌린다** — 승인 → 편성 → 착수. 로컬 모델(사내 GPU) 조직이면
  지금도 가능하다.
* **API 키를 넣는다**(T5) — `aoc-dev` 처럼 EG 가 클라우드 모델을 배정한 조직이 막혀 있다.

20~30건쯤 모여야 KPI 가 의미를 갖고, 그때 자율화 등급(현재 전원 A1) 승급을 논할 수 있다.

### T13. 자산 0개 도구의 페일세이프 (Q7 4단계)

**누가**: 에이전트 · **상태**: 사용자가 "나중에" 로 미룸

`touches` 를 카탈로그 필수로 만들어 현재 구멍은 막혔다. 남은 것은 **혹시 또
빠졌을 때** 위험도 기반 최소 심각도를 주는 페일세이프다. 지금은 카탈로그 로더가
누락을 거부하므로 실질적 위험은 없다.

### T14. 분산 배치 계획

**누가**: 사람 (판단) · **상태**: 사용자가 "나중에" 로 미룸

지금 전부 한 호스트(Intel N150 · 4코어 · 15GB)에서 돈다. 2배 스펙 시스템으로
이관 예정이라고 하셨다. 이관 전에 **무엇을 어디에 둘지**를 정해 두면 그때 한 번에
옮길 수 있다.

---

### T16. 디지털 트윈 (P8 — 수집 계층 완료, 예측 대기)

**누가**: 사람 (착수 시점 판단)

**2026-08-03 — 수집 계층은 섰다.** `Judgment` 노드가 사유 붙는 결정에서 자동으로
쌓이고, 본인이 `/judgments` 에서 보고 지울 수 있고, `eg_search` 와 대화 패널이
판례를 근거로 쓴다. 남은 것은 **예측과 발산 측정**인데 그건 표본이 차야 한다.

설계는 [`docs/instructions/P8_digital_twin.md`](docs/instructions/P8_digital_twin.md).

요지: **시스템 트윈은 이미 절반 있고**(EG Task 374·Experience 143·텔레메트리),
**사람 트윈은 거의 없다**(결재 5건). 그런데 이 회사는 **모든 변경에 사유를 요구**
하므로 판단 말뭉치가 이미 흐르고 있다 — 새로 수집할 게 아니라 노드로 만들면 된다.

**트윈의 일은 예측이 아니라 어긋남 측정이다.** 적힌 원칙과 실제 판단의 간격은
지금 안 보인다. 그 간격이 자율화 승급의 근거가 된다.

착수 조건 5개 중 **1·4 가 병목**이고 둘 다 "실업무가 돌아야 한다"로 같다 (T12).
2·3·5 는 찼다. 현재 실업무 판단 **1/20**, judge 표본 **2/20**.

조건 3 을 채우며 드러난 것: 어제 세어 둔 "실업무 판단 6건"은 틀렸다 — 전부
`p4-test` 잔재였고 실제는 0건이었다. 태그만 넣고 백필을 안 하면 필터가 조용히
아무 일도 하지 않는다.

---

### T17. 작업 지시 자동화 — 남은 절반

**누가**: 사람 (설계 확정) · 에이전트 (구현)

2026-08-04 에 ①인프라(worker 배치) ②자동 편성(`dawn-biz hire`)까지 왔다.
설계상 남은 것:

| | |
|---|---|
| 본부장 조정·승인 **화면** | 지금은 YAML 파일 편집 + CLI 승인 |
| 결정사항 관제 표시 | 없음 — 무엇을 정해야 하는지 화면에 안 뜬다 |
| 팀장 auto / 본부장 manual 분기 | 초안에 `lead` 는 적히지만 **아무도 안 읽는다** |

`lead: true` 가 지금은 표시일 뿐이다. 이걸 실제 분기로 만들려면 "결정"이라는
것이 시스템에 존재해야 하는데(지금은 HITL 승인 큐뿐), 그게 T17 의 핵심이다.

---

## 알려진 한계 — 기록만 (지금 고칠 필요 없음)

| | |
|---|---|
| 테스트가 실 트리에 쓴다 | 의도된 설계(실 매니페스트 검증). **계정 저장소만 복원 장치를 걸었다** — 승인 큐·EG 등 나머지는 그대로다 |
| 브라우저 렌더링 미검증 | 픽셀 오피스는 헤드리스 하네스 + PNG 프리뷰로만 확인했다. 실제 브라우저에서 본 적이 없다 |
| 인프라 점유 시간 미측정 | 원장에 시작 시각이 없다. 틀린 숫자보다 없는 숫자가 낫다고 판단해 안 쟀다 |
| 임시 터널 주소 | 재기동마다 URL 이 바뀐다 (T3). 지금은 터널을 닫아 둔 상태 |
| 자율화 전원 A1 | 승급은 KPI 충족 시에만 (T12) |

---

## 지금 상태 확인

```bash
make ops-status     # 전 계층 한 장 (조직·에이전트·업무·인프라·상시·관제)
# 통제 평면 조정은 그룹웨어에서:  http://<호스트>:8811/control
make standing       # 상시 작업 마지막 회차
make verify-p7      # 작업 지시 파이프라인 E2E
make check          # lint + test + control-lint + registry (CI 와 동일)
make tunnel-status  # 열려 있는 외부 URL (지금은 없음)
```
